import React from 'react';

const CurrencySettingsView = () => {
  // This component's functionality has been moved directly into the Header sidebar for easier access.
  return null;
};

export default CurrencySettingsView;
